dependencies = [
    "torch>=2.0.0",
]

pip_dependencies = [
    "torch>=2.0.0",
]

# Testing dependencies (not required for normal use)
test_dependencies = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
] 